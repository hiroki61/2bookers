<h1>Editing Book</h1>
<%= form_for(@book) do |f| %>
    <p>Title<p>
    <%= f.text_field :tit%>
    <p>Body<p>
    <%= f.text_area :bod%>
    <p> </p>
    <%= f.submit 'Update Book' %>
    <%=link_to 'Show' , "/books/#{@book.id}" %>
    <%=link_to 'Back' , "/books" %>
<% end %>
